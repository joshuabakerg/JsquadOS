--[[
function write(file,contents)
	myFile = fs.open(file,"w")
	myFile.write(contents)
	myFile.close()
	return "Suceeded"
end

function read(file)
	if fs.exists(file) then
		myFile = fs.open(file,"r")
		return myFile.readAll()
		myFile.close()
	else
		return "ErrorDoesNotExist"	
	end
end
--]]

